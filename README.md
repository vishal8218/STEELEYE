
# STEELEYE

 1.List is having four attribute index, isSelected, onclickhandler, text
index used for item number ,isSelected used for background colour (green/red), onclickhandler is enables to call function / trigger action when click on it.
 2. Warning is occured in line-38 where we used useeffect so i am comment that code .
 3. Pass the parameters in line-34 , define a functional component name is" WrappedListComponent()" and pass props as parameter" ,add div tag in the line 17 and 50.

